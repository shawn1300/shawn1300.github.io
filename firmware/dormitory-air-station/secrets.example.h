#pragma once

// Copy this file to secrets.h and replace all three values locally.
// secrets.h is ignored by Git and must never be committed.
constexpr char WIFI_SSID[] = "your-2.4-ghz-wifi-name";
constexpr char WIFI_PASSWORD[] = "your-wifi-password";
constexpr char SOURCE_TOKEN[] = "your-dormitory-esp32-source-token";
